// Generated by CoffeeScript 1.4.0
(function() {
  var Request, Response, default_charset, default_form_enctype, errorlet, loglet, multipart_form_data, util, _;

  util = require('./util');

  _ = require('underscore');

  Response = require('./response');

  loglet = require('loglet');

  errorlet = require('errorlet');

  default_charset = 'utf-8';

  default_form_enctype = 'application/x-www-form-urlencoded';

  multipart_form_data = 'multipart/form-data';

  Request = (function() {

    Request.fromForm = function(form, evt, app) {
      var charset, enctype;
      charset = $(form).prop('accept-charset') || default_charset;
      enctype = $(form).prop('enctype') || default_form_enctype;
      return new this({
        method: $(form).prop('method'),
        url: $(form).prop('action'),
        body: $(form).formValue(evt),
        headers: {
          'Content-Type': enctype === multipart_form_data ? false : "" + enctype + "; charset=" + charset
        },
        target: $(form).prop('target') || '_self',
        app: app
      });
    };

    Request.fromAnchor = function(form, evt, app) {
      return new this({
        method: 'get',
        url: $(form).prop('href'),
        lang: $(form).prop('hreflang') || 'en',
        type: $(form).prop('type') || 'text/html',
        download: $(form).prop('download') || false,
        media: $(form).prop('media'),
        rel: $(form).prop('rel'),
        target: $(form).prop('target'),
        app: app
      });
    };

    Request.fromPopState = function(evt, app) {
      var _ref;
      return new this({
        method: 'get',
        url: ((_ref = evt.originalEvent.state) != null ? _ref.url : void 0) || evt.originalEvent.explicitOriginalTarget.location.href,
        app: app,
        popState: evt.originalEvent.state
      });
    };

    function Request(params) {
      var parsed;
      _.extend(this, params);
      parsed = util.parse(this.url);
      this.query = _.extend(this.params || {}, parsed.query || {});
      this.headers || (this.headers = {});
      this.params || (this.params = {});
    }

    Request.prototype.header = function(key, val) {};

    Request.prototype.param = function(key, val) {
      if (this.params.hasOwnProperty(key)) {
        return this.params[key];
      } else if (this.body.hasOwnProperty(key)) {
        return this.body[key];
      } else {
        return this.query[key];
      }
    };

    Request.prototype.forward = function(cb) {
      switch (this.method) {
        case 'get':
          return this.get(cb);
        case 'post':
          return this.post(cb);
        default:
          return cb(null, errorlet.create({
            error: 'unimplemented_http_request',
            method: this.method,
            request: this
          }));
      }
    };

    Request.prototype.post = function(cb) {
      var $, data, req;
      $ = this.app.$;
      req = this;
      data = this.app.normalizeData(this.body);
      return $.ajax({
        type: 'post',
        url: this.url,
        headers: this.headers,
        data: data,
        success: function(data, status, xhr) {
          var res;
          try {
            res = Response.createSuccess(req, xhr, data);
            return cb(null, res);
          } catch (e) {
            return cb(e);
          }
        },
        error: function(xhr, status, error) {
          var res;
          try {
            res = Response.createError(req, xhr, data);
            return cb(null, res);
          } catch (e) {
            return cb(e);
          }
        }
      });
    };

    Request.prototype.get = function(cb) {
      var $, data, req;
      $ = this.app.$;
      req = this;
      data = this.app.normalizeData(this.body);
      return $.ajax({
        type: 'get',
        url: this.url,
        headers: this.headers,
        data: data,
        success: function(data, status, xhr) {
          var res;
          try {
            res = Response.createSuccess(req, xhr, data);
            return cb(null, res);
          } catch (e) {
            return cb(e);
          }
        },
        error: function(xhr, status, error) {
          var res;
          loglet.log('request.get', req.url, status, error);
          try {
            res = Response.createError(req, xhr, error);
            return cb(null, res);
          } catch (e) {
            return cb(e);
          }
        }
      });
    };

    return Request;

  })();

  module.exports = Request;

}).call(this);
